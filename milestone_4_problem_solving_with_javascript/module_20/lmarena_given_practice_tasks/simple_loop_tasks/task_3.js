/**
 * ৩. অ্যারের সব সংখ্যার যোগফল
 *
 * নিচের অ্যারেটি নাও এবং একটি লুপ ব্যবহার করে এর ভেতরের সবগুলা সংখ্যার যোগফল বের করো।
 * let numbers = [10, 25, 47, 8, 19, 32];
 */

let numbers = [10, 25, 47, 8, 19, 32];
let sum = 0;

for (const num of numbers) {
  sum += num;
}
console.log(sum);
